"""
DukaanAI — product_tools (Phase 5 stub)

Implement in Phase 5. Each function here must:
1. Validate input
2. Query the database via database.db.get_connection()
3. Return structured data (dict/list), never raw LLM text
4. Never hallucinate — return None / an explicit "not found" rather than guessing
"""
